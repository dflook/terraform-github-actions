rules:
  secrets-outside-env:
    disable: true
  unpinned-images:
    # action.yaml files reference ../image/Dockerfile for local builds; the
    # image is pinned to a digest during the release process.
    disable: true
